{
	"version": 2,
	"buildCommand": "yarn install && yarn prisma generate && yarn build",
	"builds": [
		{
			"src": "api/index.ts",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "api/index.ts"
		}
	],
	"env": {
		"NODE_ENV": "production"
	}
}
